import React from 'react';
import { useDispatch, useSelector } from 'react-redux';
import CounterContainer from './containers/CounterContainer';

const MwContainer = () => <CounterContainer />;

export default MwContainer;
